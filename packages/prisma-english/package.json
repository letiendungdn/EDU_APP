{
  "name": "@edu/prisma-english",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push",
    "seed": "ts-node seed.ts",
    "studio": "prisma studio"
  },
  "prisma": {
    "seed": "ts-node seed.ts"
  },
  "devDependencies": {
    "@prisma/client": "6.4.1",
    "prisma": "6.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@edu/vocab-images": "*"
  }
}
