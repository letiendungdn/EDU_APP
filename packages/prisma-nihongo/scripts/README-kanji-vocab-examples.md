# Kanji related-vocab example sentences (KanjiVocab.exampleJa/Kana/Vi)
#
# Apply schema + backfill from Vocabulary:
#   npx prisma migrate deploy --schema packages/prisma-nihongo/schema.prisma
#
# Fill remaining (templates + curated 一…):
#   npm run seed:kanji-vocab-examples -w @edu/prisma-nihongo
#   FORCE_KANJI_VOCAB_EXAMPLES_SEED=1 … để ghi đè.
