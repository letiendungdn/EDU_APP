# gRPC nội bộ — không expose ra Ingress
apiVersion: v1
kind: Service
metadata:
  name: exam-service
  namespace: edu-app
  labels:
    app: exam-service
    app.kubernetes.io/part-of: edu-platform
spec:
  type: ClusterIP
  ports:
    - port: 50052
      targetPort: 50052
      name: grpc
      protocol: TCP
  selector:
    app: exam-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: exam-service
  namespace: edu-app
  labels:
    app: exam-service
    app.kubernetes.io/part-of: edu-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exam-service
  template:
    metadata:
      labels:
        app: exam-service
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: exam-service
          image: ghcr.io/letiendungdn/edu_app/exam-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50052
              name: grpc
          envFrom:
            - configMapRef:
                name: edu-app-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: edu-app-secrets
                  key: DATABASE_URL_NIHONGO
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 3
          readinessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
